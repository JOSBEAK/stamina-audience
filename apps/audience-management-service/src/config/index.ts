/**
 * Configuration factories for the audience management service
 *
 * Exports all configuration factory functions to keep app.module.ts clean
 */
export { createDatabaseConfig } from './database.config';
export { createUploadsConfig } from './uploads.config';
